lottie.loadAnimation({
    container: document.querySelector(".imglottie"), // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'coins.json' // the path to the animation json
  });

  lottie.loadAnimation({
    container: document.querySelector(".coin-img"), // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'coins.json' // the path to the animation json
  });